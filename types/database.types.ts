// Placeholder — regenerated from the real schema once migrations are written
// (Phase 3) and, ultimately, from a live Supabase project via:
//   npm run db:types
// (wraps `supabase gen types typescript --local`)
export type Database = Record<string, never>;
